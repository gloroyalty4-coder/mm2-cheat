local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Riel Hub | Developer kitaroriel",
   LoadingTitle = "Riel Hub",
   LoadingSubtitle = "by kitaroriel",
   ConfigurationSaving = {
      Enabled = true,
      FolderName = "RielHub", 
      FileName = "Config"
   },
   KeySystem = false, 
})

-- [[ SIDEBAR TABS ]] --

-- 1. Home
local HomeTab = Window:CreateTab("Home", 4483362458) -- Home Icon

-- 2. Main (The <> Code Icon)
local MainTab = Window:CreateTab("Main", 4483345998) 

-- 3. Target (The Crosshair Icon)
local TargetTab = Window:CreateTab("Target", 4483362458) 

-- 4. Troll (The Smiley Face Icon - Your Screenshot)
local TrollTab = Window:CreateTab("Troll", 4483345998) 

-- 5. Spectate (The Eye Icon)
local SpectateTab = Window:CreateTab("Spectate", 4483345998) 

-- 6. Anim (The Person/Running Icon)
local AnimTab = Window:CreateTab("Anim", 4483345998) 

-- 7. Emotes (The Controller/Gamepad Icon)
local EmotesTab = Window:CreateTab("Emotes", 4483345998) 

-- 8. Settings (The Gear Icon)
local SettingsTab = Window:CreateTab("Settings", 4483345998) 

-- [[ ADDING THE CONTENT TO THE TROLL TAB ]] --
-- This matches your screenshot exactly
local TrollSection = TrollTab:CreateSection("Actions")

TrollTab:CreateInput({
   Name = "Troll Target Username",
   PlaceholderText = "Enter troll target...",
   Callback = function(Text)
      print("Targeting: " .. Text)
   end,
})

TrollTab:CreateButton({
   Name = "Void Target (Kill)",
   Callback = function()
      print("Executing Void...")
   end,
})

TrollTab:CreateButton({
   Name = "Loop Fling Target",
   Callback = function()
      print("Executing Fling...")
   end,
})

TrollTab:CreateToggle({
   Name = "Walk On Air (Smart)",
   CurrentValue = false,
   Callback = function(Value)
      print("Walk on Air: ", Value)
   end,
})

Rayfield:Notify({
   Title = "Riel Hub Loaded",
   Content = "Welcome back, developer kitaroriel!",
   Duration = 5,
   Image = 4483345998,
})
