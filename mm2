local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Riel Hub | Developer kitaroriel",
   LoadingTitle = "Riel Hub V2",
   LoadingSubtitle = "by kitaroriel",
   ConfigurationSaving = {Enabled = true, FolderName = "RielHub", FileName = "Config"},
   KeySystem = false, 
})

-- [[ GLOBAL VARIABLES & SERVICES ]] --
local Player = game.Players.LocalPlayer
local Camera = workspace.CurrentCamera
local RunService = game:GetService("RunService")
local TeleportService = game:GetService("TeleportService")

_G.TargetName = ""
_G.TrollTarget = ""
_G.SpeedEnabled = false
_G.WalkSpeed = 16
_G.JumpEnabled = false
_G.JumpPower = 50
_G.Noclip = false

-- [[ CORE FUNCTIONS ]] --
local function GetPlayer(Name)
    for _, v in pairs(game.Players:GetPlayers()) do
        if v.Name:lower():sub(1, #Name) == Name:lower() or v.DisplayName:lower():sub(1, #Name) == Name:lower() then
            return v
        end
    end
    return nil
end

-- Persistent Movement Loop
RunService.Stepped:Connect(function()
    if Player.Character and Player.Character:FindFirstChildOfClass("Humanoid") then
        local Hum = Player.Character:FindFirstChildOfClass("Humanoid")
        if _G.SpeedEnabled then Hum.WalkSpeed = _G.WalkSpeed end
        if _G.JumpEnabled then Hum.UseJumpPower = true; Hum.JumpPower = _G.JumpPower end
        if _G.Noclip then
            for _, v in pairs(Player.Character:GetDescendants()) do
                if v:IsA("BasePart") then v.CanCollide = false end
            end
        end
    end
end)

-- [[ SPECTATE OVERLAY UI ]] --
local SpectateGui = Instance.new("ScreenGui", game.CoreGui)
SpectateGui.Name = "RielSpectate"
SpectateGui.Enabled = false

local Bar = Instance.new("Frame", SpectateGui)
Bar.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Bar.Position = UDim2.new(0.5, -150, 0.85, 0)
Bar.Size = UDim2.new(0, 300, 0, 50)
Instance.new("UICorner", Bar).CornerRadius = UDim.new(0, 10)
local Stroke = Instance.new("UIStroke", Bar)
Stroke.Color = Color3.fromRGB(0, 255, 255)
Stroke.Thickness = 2

local function CreateSpecBtn(text, pos, color, func)
    local b = Instance.new("TextButton", Bar)
    b.Size = UDim2.new(0, 80, 0, 40)
    b.Position = pos
    b.BackgroundColor3 = color
    b.Text = text
    b.TextColor3 = Color3.new(1,1,1)
    b.Font = Enum.Font.GothamBold
    Instance.new("UICorner", b)
    b.MouseButton1Click:Connect(func)
end

local SpectateIndex = 1
local function UpdateSpec(dir)
    local plrs = game.Players:GetPlayers()
    SpectateIndex = (SpectateIndex + dir - 1) % #plrs + 1
    local target = plrs[SpectateIndex]
    if target and target.Character then Camera.CameraSubject = target.Character:FindFirstChildOfClass("Humanoid") end
end

CreateSpecBtn("< PREV", UDim2.new(0.05, 0, 0.1, 0), Color3.fromRGB(45, 45, 45), function() UpdateSpec(-1) end)
CreateSpecBtn("CLOSE", UDim2.new(0.37, 0, 0.1, 0), Color3.fromRGB(255, 60, 60), function() 
    SpectateGui.Enabled = false
    Camera.CameraSubject = Player.Character:FindFirstChildOfClass("Humanoid")
end)
CreateSpecBtn("NEXT >", UDim2.new(0.68, 0, 0.1, 0), Color3.fromRGB(45, 45, 45), function() UpdateSpec(1) end)

-- [[ TABS ]] --
local HomeTab = Window:CreateTab("Home", "home")
local MainTab = Window:CreateTab("Main", "code")
local TargetTab = Window:CreateTab("Target", "crosshair")
local TrollTab = Window:CreateTab("Troll", "smile")
local SpectateTab = Window:CreateTab("Spectate", "eye")
local AnimTab = Window:CreateTab("Anim", "user")
local SettingsTab = Window:CreateTab("Settings", "settings")

-- [[ HOME - ACCOUNT INFO ]] --
HomeTab:CreateSection("Account Info")
HomeTab:CreateLabel("Welcome, " .. Player.DisplayName)
HomeTab:CreateLabel("User ID: " .. Player.UserId)
HomeTab:CreateLabel("Account Age: " .. Player.AccountAge .. " Days")
HomeTab:CreateButton({
   Name = "Rejoin Server",
   Interact = 'Refresh koneksi server',
   Callback = function() TeleportService:Teleport(game.PlaceId, Player) end,
})

-- [[ MAIN - MOVEMENT & UTILITY ]] --
MainTab:CreateSection("Movement")
MainTab:CreateToggle({Name = "Enable Speed", Interact = "Aktifkan kecepatan lari", Callback = function(V) _G.SpeedEnabled = V end})
MainTab:CreateSlider({Name = "WalkSpeed Value", Range = {16, 300}, CurrentValue = 16, Callback = function(V) _G.WalkSpeed = V end})
MainTab:CreateToggle({Name = "Enable Jump Power", Interact = "Aktifkan kekuatan lompat", Callback = function(V) _G.JumpEnabled = V end})
MainTab:CreateSlider({Name = "JumpPower Value", Range = {50, 500}, CurrentValue = 50, Callback = function(V) _G.JumpPower = V end})

MainTab:CreateSection("Utility")
MainTab:CreateToggle({Name = "Noclip", Interact = "Tembus tembok", Callback = function(V) _G.Noclip = V end})
MainTab:CreateToggle({Name = "Fly Mode (Auraverse)", Interact = "Terbang dengan gaya Auraverse", Callback = function(V) end})

-- [[ TARGET - SELECTION & CARRY ]] --
TargetTab:CreateSection("Target Selection")
TargetTab:CreateInput({Name = "Target Username", PlaceholderText = "Enter name...", Interact = "Masukkan nama player (singkatan bisa)", Callback = function(T) _G.TargetName = T end})
TargetTab:CreateButton({
    Name = "Teleport to Target", 
    Interact = "Teleportasi instan ke posisi target",
    Callback = function()
        local t = GetPlayer(_G.TargetName)
        if t and t.Character then Player.Character:MoveTo(t.Character.HumanoidRootPart.Position) end
    end
})

TargetTab:CreateSection("Carry & Sit")
TargetTab:CreateToggle({Name = "Backpack (Sit on Back)", Interact = "Duduk di punggung target", Callback = function(V) end})
TargetTab:CreateToggle({Name = "Sit on Head", Interact = "Duduk di kepala target", Callback = function(V) end})

-- [[ TROLL - ACTIONS ]] --
TrollTab:CreateSection("Troll Actions")
TrollTab:CreateInput({Name = "Troll Target", PlaceholderText = "Username...", Callback = function(T) _G.TrollTarget = T end})
TrollTab:CreateButton({Name = "Fling Target", Callback = function() print("Flinging " .. _G.TrollTarget) end})
TrollTab:CreateToggle({Name = "Walk On Air", Interact = "Lantai muncul otomatis", Callback = function(V) end})

-- [[ SPECTATE ]] --
SpectateTab:CreateSection("Spectator Mode")
SpectateTab:CreateToggle({
   Name = "Enable Custom Spectator",
   Interact = "Spectate dengan tampilan Overlay Modern",
   Callback = function(V) 
       SpectateGui.Enabled = V 
       if not V then Camera.CameraSubject = Player.Character:FindFirstChildOfClass("Humanoid") end 
   end,
})

-- [[ ANIM ]] --
AnimTab:CreateSection("Animation Packs")
AnimTab:CreateDropdown({
   Name = "Select Animation Pack",
   Options = {"Original", "Old School", "Mage", "Ninja", "Zombie", "Knight"},
   CurrentOption = {"Original"},
   Interact = "Ganti gaya jalan/lari/diam",
   Callback = function(Option) end,
})
AnimTab:CreateButton({Name = "Reset Character", Interact = "Bunuh diri untuk reset animasi", Callback = function() Player.Character:BreakJoints() end})

-- [[ SETTINGS - LIVE THEME UPDATER ]] --
SettingsTab:CreateSection("Interface Theme")
SettingsTab:CreateDropdown({
   Name = "Change Theme",
   Options = {"Default", "AmberGlow", "GreenLight", "Ocean", "Serenity", "BloodTheme"},
   CurrentOption = {"Default"},
   Interact = "Pilih tema warna favoritmu",
   Callback = function(Option) 
       Window:SetTheme(Option[1]) -- Changes the UI color live
   end,
})
SettingsTab:CreateSection("Menu Config")
SettingsTab:CreateButton({Name = "Destroy UI", Callback = function() Rayfield:Destroy() end})

-- Initial Notification
Rayfield:Notify({
   Title = Player.DisplayName,
   Content = "Riel Hub V2 Loaded Successfully",
   Duration = 5,
   Image = "rbxthumb://type=AvatarHeadShot&id=" .. Player.UserId .. "&w=150&h=150",
})
