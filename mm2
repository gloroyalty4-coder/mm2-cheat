local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Riel Hub | Developer kitaroriel",
   LoadingTitle = "Riel Hub V3 Premium",
   LoadingSubtitle = "by kitaroriel",
   ConfigurationSaving = {Enabled = true, FolderName = "RielHub", FileName = "Config"},
   KeySystem = false, 
})

-- [[ GLOBAL VARIABLES & LOGIC ]] --
local Player = game.Players.LocalPlayer
local Camera = workspace.CurrentCamera
local RunService = game:GetService("RunService")
local TeleportService = game:GetService("TeleportService")

_G.TargetName = ""
_G.TrollTarget = ""
_G.SpeedEnabled = false
_G.WalkSpeed = 16
_G.JumpEnabled = false
_G.JumpPower = 50
_G.Noclip = false

-- Function: Find Player by Name/Display Name
local function GetPlayer(Name)
    Name = Name:lower()
    for _, v in pairs(game.Players:GetPlayers()) do
        if v.Name:lower():sub(1, #Name) == Name or v.DisplayName:lower():sub(1, #Name) == Name then
            return v
        end
    end
    return nil
end

-- Core Engine Loop (Speed, Jump, Noclip)
RunService.Stepped:Connect(function()
    pcall(function()
        if Player.Character and Player.Character:FindFirstChildOfClass("Humanoid") then
            local Hum = Player.Character:FindFirstChildOfClass("Humanoid")
            if _G.SpeedEnabled then Hum.WalkSpeed = _G.WalkSpeed end
            if _G.JumpEnabled then 
                Hum.UseJumpPower = true
                Hum.JumpPower = _G.JumpPower 
            end
            if _G.Noclip then
                for _, v in pairs(Player.Character:GetDescendants()) do
                    if v:IsA("BasePart") then v.CanCollide = false end
                end
            end
        end
    end)
end)

-- [[ SPECTATE OVERLAY UI ]] --
local SpectateGui = Instance.new("ScreenGui", game.CoreGui)
SpectateGui.Name = "RielSpectateOverlay"
SpectateGui.Enabled = false

local Bar = Instance.new("Frame", SpectateGui)
Bar.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
Bar.Position = UDim2.new(0.5, -150, 0.88, 0)
Bar.Size = UDim2.new(0, 300, 0, 50)
Instance.new("UICorner", Bar).CornerRadius = UDim.new(0, 8)
local Stroke = Instance.new("UIStroke", Bar)
Stroke.Color = Color3.fromRGB(0, 240, 255)
Stroke.Thickness = 2

local function CreateSpecBtn(text, pos, color, func)
    local b = Instance.new("TextButton", Bar)
    b.Size = UDim2.new(0, 85, 0, 38)
    b.Position = pos
    b.BackgroundColor3 = color
    b.Text = text
    b.TextColor3 = Color3.new(1,1,1)
    b.Font = Enum.Font.GothamBold
    b.TextSize = 12
    Instance.new("UICorner", b).CornerRadius = UDim.new(0, 6)
    b.MouseButton1Click:Connect(func)
end

local SpectateIndex = 1
local function UpdateSpec(dir)
    local plrs = game.Players:GetPlayers()
    if #plrs == 0 then return end
    SpectateIndex = (SpectateIndex + dir - 1) % #plrs + 1
    local target = plrs[SpectateIndex]
    if target and target.Character then Camera.CameraSubject = target.Character:FindFirstChildOfClass("Humanoid") end
end

CreateSpecBtn("< PREV", UDim2.new(0.03, 0, 0.12, 0), Color3.fromRGB(35, 35, 35), function() UpdateSpec(-1) end)
CreateSpecBtn("CLOSE", UDim2.new(0.36, 0, 0.12, 0), Color3.fromRGB(255, 45, 45), function() 
    SpectateGui.Enabled = false
    Camera.CameraSubject = Player.Character:FindFirstChildOfClass("Humanoid")
end)
CreateSpecBtn("NEXT >", UDim2.new(0.69, 0, 0.12, 0), Color3.fromRGB(35, 35, 35), function() UpdateSpec(1) end)

-- [[ TAB INITIALIZATION ]] --
local HomeTab = Window:CreateTab("Home", "home")
local MainTab = Window:CreateTab("Main", "code")
local TargetTab = Window:CreateTab("Target", "crosshair")
local TrollTab = Window:CreateTab("Troll", "smile")
local SpectateTab = Window:CreateTab("Spectate", "eye")
local AnimTab = Window:CreateTab("Anim", "user")
local SettingsTab = Window:CreateTab("Settings", "settings")

-- [[ HOME - REAL ACCOUNT DATA & DISCORD ]] --
HomeTab:CreateSection("Account Info")
HomeTab:CreateLabel("Username: " .. Player.Name)
HomeTab:CreateLabel("Display: " .. Player.DisplayName)
HomeTab:CreateLabel("User ID: " .. Player.UserId)

HomeTab:CreateSection("Community")
local DiscordLink = "https://discord.gg/rielhub" -- YOUR LINK HERE
HomeTab:CreateInput({
    Name = "Discord Server",
    PlaceholderText = DiscordLink,
    RemoveTextAfterFocusLost = false,
    Callback = function() end,
})
HomeTab:CreateButton({
   Name = "Copy Discord Link",
   Interact = 'Salin ke clipboard',
   Callback = function()
       setclipboard(DiscordLink)
       Rayfield:Notify({
           Title = "Copied!",
           Content = "Discord link copied to clipboard.",
           Duration = 3,
           Image = 4483362458
       })
   end,
})

HomeTab:CreateSection("Server")
HomeTab:CreateButton({
   Name = "Rejoin Server",
   Interact = 'Refresh koneksi server',
   Callback = function() TeleportService:Teleport(game.PlaceId, Player) end,
})

-- [[ MAIN - MOVEMENT ]] --
MainTab:CreateSection("Movement")
MainTab:CreateToggle({
    Name = "Enable Speed",
    CurrentValue = false,
    Callback = function(V) _G.SpeedEnabled = V end,
})
MainTab:CreateSlider({
    Name = "WalkSpeed Value",
    Range = {16, 300},
    Increment = 1,
    CurrentValue = 16,
    Callback = function(V) _G.WalkSpeed = V end,
})
MainTab:CreateToggle({
    Name = "Enable Jump Power",
    CurrentValue = false,
    Callback = function(V) _G.JumpEnabled = V end,
})
MainTab:CreateSlider({
    Name = "JumpPower Value",
    Range = {50, 500},
    Increment = 1,
    CurrentValue = 50,
    Callback = function(V) _G.JumpPower = V end,
})

MainTab:CreateSection("Utility")
MainTab:CreateToggle({Name = "Noclip", CurrentValue = false, Callback = function(V) _G.Noclip = V end})

-- [[ TARGET - SELECTION & TELEPORT ]] --
TargetTab:CreateSection("Target Selection")
TargetTab:CreateInput({
    Name = "Target Username",
    PlaceholderText = "Enter name...",
    Callback = function(T) _G.TargetName = T end,
})
TargetTab:CreateButton({
    Name = "Teleport to Target", 
    Callback = function()
        local t = GetPlayer(_G.TargetName)
        if t and t.Character then 
            Player.Character.HumanoidRootPart.CFrame = t.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -3)
        end
    end
})

TargetTab:CreateSection("Carry & Sit")
TargetTab:CreateToggle({Name = "Backpack (Sit on Back)", CurrentValue = false, Callback = function(V) end})
TargetTab:CreateToggle({Name = "Sit on Head", CurrentValue = false, Callback = function(V) end})

-- [[ TROLL - ACTIONS ]] --
TrollTab:CreateSection("Troll Actions")
TrollTab:CreateInput({Name = "Troll Target", PlaceholderText = "Enter target...", Callback = function(T) _G.TrollTarget = T end})
TrollTab:CreateButton({
    Name = "Fling Target",
    Callback = function()
        local t = GetPlayer(_G.TrollTarget)
        if t and t.Character then
            local hrp = Player.Character.HumanoidRootPart
            local thrp = t.Character.HumanoidRootPart
            local oldPos = hrp.CFrame
            hrp.CFrame = thrp.CFrame
            hrp.Velocity = Vector3.new(999999, 999999, 999999)
            task.wait(0.2)
            hrp.Velocity = Vector3.new(0,0,0)
            hrp.CFrame = oldPos
        end
    end
})
TrollTab:CreateToggle({Name = "Walk On Air", CurrentValue = false, Callback = function(V) end})

-- [[ SPECTATE ]] --
SpectateTab:CreateSection("Spectator Mode")
SpectateTab:CreateToggle({
   Name = "Enable Custom Spectator",
   CurrentValue = false,
   Callback = function(V) 
       SpectateGui.Enabled = V 
       if not V then Camera.CameraSubject = Player.Character:FindFirstChildOfClass("Humanoid") end 
   end,
})

-- [[ ANIMATIONS ]] --
AnimTab:CreateSection("Animation Packs")
AnimTab:CreateDropdown({
   Name = "Select Animation Pack",
   Options = {"Original", "Old School", "Mage", "Ninja", "Zombie", "Knight"},
   CurrentOption = {"Original"},
   Callback = function(Option) end,
})
AnimTab:CreateButton({Name = "Reset Character", Callback = function() Player.Character:BreakJoints() end})

-- [[ SETTINGS - LIVE THEME UPDATER ]] --
SettingsTab:CreateSection("Interface Theme")
SettingsTab:CreateDropdown({
   Name = "Change Theme",
   Options = {"Default", "AmberGlow", "GreenLight", "Ocean", "Serenity", "BloodTheme"},
   CurrentOption = {"Default"},
   Callback = function(Option) 
       Window:SetTheme(Option[1]) -- Updates UI colors live
   end,
})

SettingsTab:CreateSection("Menu Config")
SettingsTab:CreateButton({Name = "Destroy UI", Callback = function() Rayfield:Destroy() end})

Rayfield:Notify({
   Title = Player.DisplayName,
   Content = "Riel Hub V3 Premium Loaded!",
   Duration = 5,
   Image = "rbxthumb://type=AvatarHeadShot&id=" .. Player.UserId .. "&w=150&h=150",
})
