local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Riel Hub | Developer kitaroriel",
   LoadingTitle = "Riel Hub┃Free",
   LoadingSubtitle = "by kitaroriel",
   ConfigurationSaving = {Enabled = true, FolderName = "RielHub", FileName = "Config"},
   KeySystem = false,
   Theme = "Default" -- Image 2 uses the Default Rayfield top-bar theme
})

-- [[ GLOBAL VARIABLES & LOGIC ]] --
local Player = game.Players.LocalPlayer
local Camera = workspace.CurrentCamera
local RunService = game:GetService("RunService")
local TeleportService = game:GetService("TeleportService")

_G.TargetName = ""
_G.TrollTarget = ""
_G.SpeedEnabled = false
_G.WalkSpeed = 16
_G.JumpEnabled = false
_G.JumpPower = 50
_G.Noclip = false

-- Function: Find Player
local function GetPlayer(Name)
    Name = Name:lower()
    for _, v in pairs(game.Players:GetPlayers()) do
        if v.Name:lower():sub(1, #Name) == Name or v.DisplayName:lower():sub(1, #Name) == Name then
            return v
        end
    end
    return nil
end

-- Core Engine Loop (Speed, Jump, Noclip)
RunService.Stepped:Connect(function()
    pcall(function()
        if Player.Character and Player.Character:FindFirstChildOfClass("Humanoid") then
            local Hum = Player.Character:FindFirstChildOfClass("Humanoid")
            if _G.SpeedEnabled then Hum.WalkSpeed = _G.WalkSpeed end
            if _G.JumpEnabled then 
                Hum.UseJumpPower = true
                Hum.JumpPower = _G.JumpPower 
            end
            if _G.Noclip then
                for _, v in pairs(Player.Character:GetDescendants()) do
                    if v:IsA("BasePart") then v.CanCollide = false end
                end
            end
        end
    end)
end)

-- [[ TOP TAB INITIALIZATION (MATCHING IMAGE 2) ]] --
local HomeTab = Window:CreateTab("Home", "home") --
local MainTab = Window:CreateTab("Main", "terminal") --
local TargetTab = Window:CreateTab("Target", "target") --
local TrollTab = Window:CreateTab("Troll", "smile") --
local SpectateTab = Window:CreateTab("Spectate", "eye") --

-- [[ HOME - ACCOUNT INFO SECTION ]] --
HomeTab:CreateSection("Account Info") --

HomeTab:CreateButton({
   Name = "User Information",
   Interact = "button", --
   Callback = function()
       Rayfield:Notify({
           Title = "User Stats",
           Content = "Username: "..Player.Name.."\nID: "..Player.UserId,
           Duration = 5
       })
   end,
})

HomeTab:CreateButton({
   Name = "Rejoin Server",
   Interact = "button", --
   Callback = function() TeleportService:Teleport(game.PlaceId, Player) end,
})

-- [[ HOME - COMMUNITY SECTION ]] --
HomeTab:CreateSection("Community") --

HomeTab:CreateLabel("Discord Server: https://discord.gg/H6ccmTMd7T") --

HomeTab:CreateButton({
   Name = "Copy Discord Link",
   Interact = "button", --
   Callback = function()
       setclipboard("https://discord.gg/H6ccmTMd7T")
       Rayfield:Notify({Title = "Copied", Content = "Link copied to clipboard", Duration = 3})
   end,
})

-- [[ MAIN - MOVEMENT ]] --
MainTab:CreateSection("Movement")
MainTab:CreateToggle({Name = "Enable Speed", CurrentValue = false, Callback = function(V) _G.SpeedEnabled = V end})
MainTab:CreateSlider({Name = "WalkSpeed", Range = {16, 300}, Increment = 1, CurrentValue = 16, Callback = function(V) _G.WalkSpeed = V end})
MainTab:CreateToggle({Name = "Enable Jump", CurrentValue = false, Callback = function(V) _G.JumpEnabled = V end})
MainTab:CreateSection("Utility")
MainTab:CreateToggle({Name = "Noclip", CurrentValue = false, Callback = function(V) _G.Noclip = V end})

-- [[ TARGET ]] --
TargetTab:CreateSection("Target Selection")
TargetTab:CreateInput({Name = "Target Username", PlaceholderText = "Enter name...", Callback = function(T) _G.TargetName = T end})
TargetTab:CreateButton({
    Name = "Teleport to Target", 
    Callback = function()
        local t = GetPlayer(_G.TargetName)
        if t and t.Character then 
            Player.Character.HumanoidRootPart.CFrame = t.Character.HumanoidRootPart.CFrame
        end
    end
})

-- [[ TROLL ]] --
TrollTab:CreateSection("Troll Actions")
TrollTab:CreateInput({Name = "Troll Target", PlaceholderText = "Enter target...", Callback = function(T) _G.TrollTarget = T end})
TrollTab:CreateButton({
    Name = "Fling Target",
    Callback = function()
        local t = GetPlayer(_G.TrollTarget)
        if t and t.Character then
            local hrp = Player.Character.HumanoidRootPart
            local oldPos = hrp.CFrame
            hrp.CFrame = t.Character.HumanoidRootPart.CFrame
            hrp.Velocity = Vector3.new(999999, 999999, 999999)
            task.wait(0.2)
            hrp.Velocity = Vector3.new(0,0,0)
            hrp.CFrame = oldPos
        end
    end
})

-- [[ SPECTATE ]] --
SpectateTab:CreateSection("Spectator Mode")
SpectateTab:CreateButton({
    Name = "Toggle Freecam",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/GhostPlayer352/Test4/main/Freecam"))()
    end
})

Rayfield:Notify({
   Title = "Riel Hub Loaded",
   Content = "Top-navigation mode enabled.",
   Duration = 5,
   Image = 4483362458,
})
