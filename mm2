local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Riel Hub | Developer kitaroriel",
   LoadingTitle = "Riel Hub",
   LoadingSubtitle = "by kitaroriel",
   ConfigurationSaving = {Enabled = true, FolderName = "RielHub", FileName = "Config"},
   KeySystem = false, 
})

-- [[ GLOBAL VARIABLES & LOGIC ]] --
local Player = game.Players.LocalPlayer
local RunService = game:GetService("RunService")
local TeleportService = game:GetService("TeleportService")

_G.SpeedEnabled = false
_G.WalkSpeed = 16
_G.JumpEnabled = false
_G.JumpPower = 50
_G.Noclip = false

-- Core Loop to ensure Speed, Jump, and Noclip stay active
RunService.Stepped:Connect(function()
    if Player.Character and Player.Character:FindFirstChildOfClass("Humanoid") then
        local Hum = Player.Character:FindFirstChildOfClass("Humanoid")
        if _G.SpeedEnabled then Hum.WalkSpeed = _G.WalkSpeed end
        if _G.JumpEnabled then Hum.UseJumpPower = true; Hum.JumpPower = _G.JumpPower end
        if _G.Noclip then
            for _, v in pairs(Player.Character:GetDescendants()) do
                if v:IsA("BasePart") then v.CanCollide = false end
            end
        end
    end
end)

-- [[ SIDEBAR TABS ]] --
local HomeTab = Window:CreateTab("Home", "home") 
local MainTab = Window:CreateTab("Main", "code")
local TargetTab = Window:CreateTab("Target", "crosshair")
local TrollTab = Window:CreateTab("Troll", "smile")
local SpectateTab = Window:CreateTab("Spectate", "eye")
local AnimTab = Window:CreateTab("Anim", "user")
local EmotesTab = Window:CreateTab("Emotes", "gamepad-2")
local SettingsTab = Window:CreateTab("Settings", "settings")

-- [[ HOME TAB CONTENT ]] --
local AccountSection = HomeTab:CreateSection("Account Info")
HomeTab:CreateLabel("User Information")
HomeTab:CreateButton({
   Name = "Rejoin Server",
   Interact = 'Refresh koneksi server',
   Callback = function() TeleportService:Teleport(game.PlaceId, Player) end,
})

-- [[ MAIN TAB CONTENT - MOVEMENT & UTILITY ]] --
local MovementSection = MainTab:CreateSection("Movement")

MainTab:CreateToggle({
   Name = "Enable Speed",
   CurrentValue = false,
   Interact = 'Aktifkan kecepatan lari',
   Callback = function(Value) _G.SpeedEnabled = Value end,
})

MainTab:CreateSlider({
   Name = "WalkSpeed Value",
   Range = {16, 300},
   Increment = 1,
   CurrentValue = 16,
   Callback = function(Value) _G.WalkSpeed = Value end,
})

MainTab:CreateToggle({
   Name = "Enable Jump Power",
   CurrentValue = false,
   Interact = 'Aktifkan kekuatan lompat',
   Callback = function(Value) _G.JumpEnabled = Value end,
})

MainTab:CreateSlider({
   Name = "JumpPower Value",
   Range = {50, 500},
   Increment = 1,
   CurrentValue = 50,
   Callback = function(Value) _G.JumpPower = Value end,
})

local UtilitySection = MainTab:CreateSection("Utility")

MainTab:CreateToggle({
   Name = "Noclip",
   CurrentValue = false,
   Interact = 'Tembus tembok',
   Callback = function(Value) _G.Noclip = Value end,
})

MainTab:CreateToggle({
   Name = "Fly Mode (Auraverse)",
   CurrentValue = false,
   Interact = 'Terbang dengan gaya Auraverse',
   Callback = function(Value) 
       -- Fly logic toggle
   end,
})

MainTab:CreateSlider({
   Name = "Fly Speed",
   Range = {1, 500},
   Increment = 1,
   CurrentValue = 50,
   Callback = function(Value) _G.FlySpeed = Value end,
})

-- [[ TROLL TAB CONTENT ]] --
local TrollSection = TrollTab:CreateSection("Actions")

TrollTab:CreateInput({
   Name = "Troll Target Username",
   PlaceholderText = "Enter troll target...",
   Callback = function(Text) _G.TargetName = Text end,
})

TrollTab:CreateButton({
   Name = "Void Target (Kill)",
   Interact = "Teleport target troll ke void",
   Callback = function() print("Voiding " .. tostring(_G.TargetName)) end,
})

TrollTab:CreateButton({
   Name = "Loop Fling Target",
   Interact = "Fling target troll berulang kali",
   Callback = function() print("Flinging " .. tostring(_G.TargetName)) end,
})

TrollTab:CreateToggle({
   Name = "Walk On Air (Smart)",
   CurrentValue = false,
   Interact = "Lantai muncul otomatis (Mati saat Backpack)",
   Callback = function(Value) _G.WalkOnAir = Value end,
})

-- [[ SETTINGS TAB ]] --
local SettingsSection = SettingsTab:CreateSection("Interface")
SettingsTab:CreateButton({
    Name = "Destroy UI",
    Callback = function() Rayfield:Destroy() end,
})

-- [[ PROFILE NOTIFICATION ]] --
-- This mimics the "6sixig" layout at the bottom left of your image
Rayfield:Notify({
   Title = Player.DisplayName,
   Content = "@" .. Player.Name,
   Duration = 5,
   Image = "rbxthumb://type=AvatarHeadShot&id=" .. Player.UserId .. "&w=150&h=150",
})
