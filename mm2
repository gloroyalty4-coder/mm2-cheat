local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Riel Hub | Developer kitaroriel",
   LoadingTitle = "Riel Hub",
   LoadingSubtitle = "by kitaroriel",
   ConfigurationSaving = {
      Enabled = true,
      FolderName = "RielHub", 
      FileName = "Config"
   },
   Discord = {
      Enabled = false,
   },
   KeySystem = false, 
})

-- Creating the "Troll" Tab (Matching your image)
local TrollTab = Window:CreateTab("Troll", 4483345998) -- Icon ID for the smiley face

local Section = TrollTab:CreateSection("Actions")

-- 1. Troll Target Box
TrollTab:CreateInput({
   Name = "Troll Target Username",
   PlaceholderText = "Enter troll target...",
   RemoveTextAfterFocusLost = false,
   Callback = function(Text)
      print("Target set to: " .. Text)
   end,
})

-- 2. Void Target Button
TrollTab:CreateButton({
   Name = "Void Target (Kill)",
   Interact = 'Click',
   Callback = function()
      -- Logic would go here
      print("Attempting to void target...")
   end,
})

-- 3. Loop Fling Button
TrollTab:CreateButton({
   Name = "Loop Fling Target",
   Interact = 'Click',
   Callback = function()
      print("Flinging target...")
   end,
})

-- 4. Walk On Air Toggle
TrollTab:CreateToggle({
   Name = "Walk On Air (Smart)",
   CurrentValue = false,
   Flag = "WalkAir", 
   Callback = function(Value)
      print("Walk on Air is now: ", Value)
   end,
})
